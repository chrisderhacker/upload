services:
  projekt-tool:
    build: .
    container_name: projekt-tool
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
    restart: unless-stopped
